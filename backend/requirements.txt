# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.6.0

# Database
sqlalchemy==2.0.34
psycopg2-binary==2.9.9
alembic==1.13.2

# Auth & Security
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9

# HTTP client
httpx==0.27.2

# Redis
redis==5.2.0

# Environment
python-dotenv==1.0.1

# Observability
python-json-logger==2.0.7
prometheus-client==0.20.0

# Rate limiting
slowapi==0.1.9

# Email validation
email-validator==2.2.0

# Testing (dev)
pytest==8.3.3
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.2

# Optional: OpenTelemetry (set ENABLE_OTEL=true to activate)
# opentelemetry-api==1.28.0
# opentelemetry-sdk==1.28.0
# opentelemetry-exporter-otlp==1.28.0
# opentelemetry-instrumentation-fastapi==0.50b0

# Optional: AI packages (only for chart-type recommendation, must have deterministic fallback)
# openai==1.51.0
