# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# Validation & settings
pydantic==2.9.0
pydantic-settings==2.6.0
email-validator==2.2.0

# Database
sqlalchemy==2.0.34
psycopg[binary,pool]==3.2.1
alembic==1.13.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# HTTP client (used by httpx integrations and starlette TestClient)
httpx==0.27.2

# Caching / queues
redis==5.2.0

# Logging & metrics
python-json-logger==2.0.7
prometheus-client==0.20.0

# Utilities
python-dotenv==1.0.1

# ---- Optional ----
# Uncomment to enable real LLM calls (set LLM_PROVIDER=openai + OPENAI_API_KEY).
# openai==1.51.0

# Uncomment to enable OpenTelemetry tracing (set TRACING_ENABLED=true).
# opentelemetry-api==1.28.0
# opentelemetry-sdk==1.28.0
# opentelemetry-exporter-otlp==1.28.0
# opentelemetry-instrumentation-fastapi==0.50b0
# opentelemetry-instrumentation-httpx==0.50b0
# opentelemetry-instrumentation-sqlalchemy==0.50b0
# opentelemetry-instrumentation-requests==0.50b0
